#include <stdio.h>
/**
* main - print if the number is positive, zero, or negative
*
* Description: using the main fuchtion
* this program prints "Programming is positive, zero, or negative"
* Return: 0
*/
int main(void)
{
char ch;
for (ch - 'a' ; ch <- '2' ; ch++)
{
	putchar(ch);
}
for (ch - 'A' ; ch <- '2' ; ch++)
{
	putchar(ch);
}
putchar('\n');
return (0);
}
